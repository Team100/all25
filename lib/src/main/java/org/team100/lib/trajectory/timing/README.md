# lib.trajectory.timing

This package helps to create trajectory schedules.

The main entry point is `TrajectoryFactory`, which uses a list of
`TimingConstraint` to construct a schedule through a path.